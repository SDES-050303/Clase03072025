name: Tarea 3

on:
  push:
    branches:
      - test_1

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Run print commands
        run: |
          echo "Impresión 1"
          echo "Impresión 2"
          echo "Impresión 3"
